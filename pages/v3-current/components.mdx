---
title: Website Components
description: TLP's Documentation website
---
import { StarsIcon, LightningIcon, CloudIcon, CardsIcon } from '@/components/icons'
import { Image } from 'next/dist/client/image-component'
import { VersionCards } from '@/components/VersionCards'
import { siteTitle } from '@/theme.config'

{/* <Image
  src='/assets/lepetitprince.png'
  width={0}
  height={0}
  sizes='100vw'
  style={{ width: '100%', height: 'auto' }}
  alt='Le Petit Prince'
/> */}

# Website Components

The website is built out of several components. Below are the current versions of the components for `release V3`.

<VersionCards />
